service: crud-api-serverless
frameworkVersion: "3"
provider:
  name: aws
  runtime: nodejs18.x
  architecture: arm64
  region: us-east-1
  stage: ${opt:stage, 'dev'}
  environment:
    ITEMS_TABLE: ${self:service}-${self:provider.stage}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource:
        - Fn::GetAtt: [ ItemsTable, Arn ]

custom:
  stages:
    - dev
    - staging
    - prod

functions:
  createItem:
    handler: src/handlers/create.handler
    events:
      - http:
          path: items
          method: post
          cors: true

  getItems:
    handler: src/handlers/read.getAllItems
    events:
      - http:
          path: items
          method: get
          cors: true

  getItem:
    handler: src/handlers/read.getItem
    events:
      - http:
          path: items/{id}
          method: get
          cors: true

  updateItem:
    handler: src/handlers/update.handler
    events:
      - http:
          path: items/{id}
          method: put
          cors: true

  deleteItem:
    handler: src/handlers/delete.handler
    events:
      - http:
          path: items/{id}
          method: delete
          cors: true

resources:
  Resources:
    ItemsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.ITEMS_TABLE}
        AttributeDefinitions:
          - AttributeName: itemId
            AttributeType: S
        KeySchema:
          - AttributeName: itemId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

package:
  patterns:
    - '!**/*'
    - 'src/**'
    - 'node_modules/**'
    - 'package.json'

plugins:
  - serverless-offline
